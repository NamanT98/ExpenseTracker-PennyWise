export interface ExpenseSheet {
  id: string;
  name: string;
  createdAt: string;
}

export interface Testimonial {
  avatar: string;
  name: string;
  title: string;
  stars: string;
  quote: string;
}
